{
  "$schema": "../gen/schemas/desktop-capability.json",
  "identifier": "default",
  "description": "Strict file system permission — only app data directory",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:allow-app-read",
    "fs:allow-app-write"
  ]
}
